/**
 * @format
 */

import { AppRegistry } from 'react-native';
import { name as appName } from './app.json';
import { EcoApp } from './src/EcoApp';

AppRegistry.registerComponent(appName, () => EcoApp);
